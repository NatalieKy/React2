import React from 'react';

function Header(props) {
    return (
        <header>
            <div className="card">
                <div className="card-body">
                    Task app
                </div>
            </div>
        </header>
    );
}

export default Header;
